<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="/css/style.css">
    <script src="/js/main.js"></script>
    <title><%= project.name %></title>
</head>
<body>
    <header>
        <div class="brand" onclick="location.href='/'" style="cursor:pointer"><] Back</div>
    </header>

    <div class="container" style="max-width: 900px; margin: 20px auto; padding: 20px;">
        <h1 style="color: #FF0000; margin-bottom: 5px;"><%= project.name %></h1>
        <p>Language: <span style="color: #FF0000;"><%= project.language %></span></p>

        <% if (project.preview) { %>
            <img src="<%= project.preview %>" style="width: 100%; border: 1px solid #FF0000; margin: 20px 0;">
        <% } %>

        <div style="background: #111; padding: 20px; border-left: 4px solid #FF0000;">
            <h3>Catatan:</h3>
            <p><%= project.note || 'Tidak ada catatan.' %></p>
        </div>

        <h3 style="margin-top: 30px;">Source Code / File:</h3>
        <% if (project.type === 'code') { %>
            <div class="code-box" id="codeArea"><%= project.content %></div>
            <button class="btn-info" onclick="copyToClipboard('codeArea')">Salin Code</button>
        <% } %>

        <div style="margin-top: 30px; display: flex; gap: 20px; align-items: center;">
            <a href="/download/<%= project._id %>" class="btn-download">DOWNLOAD SEKARANG</a>
            <form action="/like/<%= project._id %>" method="POST">
                <button type="submit" class="btn-info" style="background: transparent; border: 1px solid #FF0000;">❤️ <%= project.likes %> Like</button>
            </form>
            <span style="color: #888;">📥 <%= project.downloads %> Kali didownload</span>
        </div>
    </div>
</body>
</html>
