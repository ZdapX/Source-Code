<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SOURCE CODE - Repository</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&family=Press+Start+2P&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <div class="logo">&lt;/&gt; SOURCE CODE</div>
        <a href="/profile" class="btn-modern">INFO 👑</a>
    </header>

    <div class="search-container">
        <form action="/" method="GET">
            <input type="text" name="search" class="search-input" placeholder="SEARCH PROJECTS_">
        </form>
    </div>

    <main class="grid">
        <% projects.forEach(p => { %>
        <div class="card" onclick="location.href='/project/<%= p._id %>'" style="cursor: pointer;">
            <div>
                <span class="card-lang"><%= p.language %></span>
                <h2 class="card-title"><%= p.name %></h2>
                <p style="color: var(--text-dim); font-size: 0.8rem;">By: <%= p.uploadedBy %></p>
            </div>
            <div class="card-stats">
                <div class="stat-item">❤️ <b><%= p.likes %></b></div>
                <div class="stat-item">📥 <b><%= p.downloads %></b></div>
            </div>
        </div>
        <% }) %>
    </main>

    <div style="position: fixed; bottom: 30px; right: 30px;">
        <a href="/chat-pilih" class="btn-modern" style="border-radius: 0; box-shadow: 6px 6px 0 white;">💬 CHAT_ADMIN</a>
    </div>
</body>
</html>
