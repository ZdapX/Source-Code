<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DATABASE_MGMT | Admin</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&family=Press+Start+2P&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .manage-wrapper {
            max-width: 1000px;
            margin: 50px auto;
            padding: 0 20px;
        }
        .header-status {
            font-family: 'Press Start 2P';
            font-size: 0.7rem;
            color: #444;
            margin-bottom: 30px;
            display: flex;
            justify-content: space-between;
        }
        .project-row {
            background: #0A0A0A;
            border-left: 5px solid #FF0000;
            border-right: 1px solid #222;
            border-top: 1px solid #222;
            border-bottom: 1px solid #222;
            padding: 25px;
            margin-bottom: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: 0.3s;
        }
        .project-row:hover {
            background: #111;
            border-right-color: #FF0000;
        }
        .proj-info h3 {
            font-size: 1.3rem;
            margin-bottom: 5px;
            text-transform: uppercase;
        }
        .proj-meta {
            font-family: 'Press Start 2P';
            font-size: 0.5rem;
            color: #FF0000;
            display: flex;
            gap: 15px;
        }
        .proj-stats {
            display: flex;
            gap: 30px;
            text-align: center;
        }
        .stat-box-mini {
            font-size: 0.8rem;
            color: #666;
        }
        .stat-box-mini b {
            display: block;
            color: #FFF;
            font-size: 1.1rem;
        }
        .btn-purge {
            background: transparent;
            border: 1px solid #FF0000;
            color: #FF0000;
            padding: 10px 20px;
            font-family: 'Press Start 2P';
            font-size: 0.5rem;
            text-decoration: none;
            transition: 0.2s;
        }
        .btn-purge:hover {
            background: #FF0000;
            color: white;
            box-shadow: 0 0 15px #FF0000;
        }
    </style>
</head>
<body>
    <header>
        <div class="logo">DATABASE_EXPLORER</div>
        <a href="/admin/upload" class="btn-modern btn-outline">+ NEW_ENTRY</a>
    </header>

    <div class="manage-wrapper">
        <div class="header-status">
            <span>ACTIVE_SESSION: <%= adminName %></span>
            <span>TOTAL_OBJECTS: <%= projects.length %></span>
        </div>

        <% if(projects.length === 0) { %>
            <div style="text-align: center; padding: 100px; border: 2px dashed #222; color: #444;">
                <p style="font-family: 'Press Start 2P'; font-size: 0.8rem;">NO_DATA_FOUND</p>
            </div>
        <% } %>

        <% projects.forEach(p => { %>
        <div class="project-row">
            <div class="proj-info">
                <div class="proj-meta">
                    <span>TYPE: <%= p.type.toUpperCase() %></span>
                    <span>LANG: <%= p.language %></span>
                </div>
                <h3><%= p.name %></h3>
            </div>

            <div class="proj-stats">
                <div class="stat-box-mini">
                    <b><%= p.likes %></b> LIKES
                </div>
                <div class="stat-box-mini">
                    <b><%= p.downloads %></b> HITS
                </div>
                <div style="display: flex; align-items: center; margin-left: 20px;">
                    <a href="/admin/delete/<%= p._id %>" class="btn-purge" onclick="return confirm('WARNING: PERMANENT PURGE?')">PURGE_DATA</a>
                </div>
            </div>
        </div>
        <% }) %>
    </div>
</body>
</html>
