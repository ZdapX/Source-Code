<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>COMMAND_ // Upload Center</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="app-container" style="max-width: 800px;">
        <header style="margin-bottom: 4rem; text-align: center;">
            <p class="mono" style="color: var(--accent); margin-bottom: 0.5rem;">[ ADMIN_PRIVILEGE_ESC ]</p>
            <h1>UPLOAD_CENTER</h1>
            
            <div style="display: flex; justify-content: center; gap: 2rem; margin-top: 2rem;">
                <a href="/admin/edit-profile" class="nav-link">⚙️ CONFIG_IDENTITY</a>
                <a href="/admin/manage" class="nav-link">📂 REPO_MANAGER</a>
            </div>
        </header>

        <div style="background: var(--zinc-900); border: 1px solid var(--zinc-800); border-radius: 4rem; padding: 4rem;">
            <form action="/admin/upload" method="POST" enctype="multipart/form-data">
                
                <div style="margin-bottom: 2.5rem;">
                    <label class="mono" style="display: block; margin-bottom: 1rem; margin-left: 1.5rem;">PROJECT_IDENTIFIER</label>
                    <input type="text" name="name" class="cyber-input" placeholder="ENTER_PROJECT_NAME" required>
                </div>

                <div style="margin-bottom: 2.5rem;">
                    <label class="mono" style="display: block; margin-bottom: 1rem; margin-left: 1.5rem;">CORE_LANGUAGE</label>
                    <input type="text" name="language" class="cyber-input" placeholder="JAVASCRIPT / PYTHON / PHP" required>
                </div>

                <div style="margin-bottom: 2.5rem;">
                    <label class="mono" style="display: block; margin-bottom: 1rem; margin-left: 1.5rem;">DISTRIBUTION_TYPE</label>
                    <select name="type" id="typeSelect" class="cyber-input" style="appearance: none;" onchange="toggleInput()">
                        <option value="file">BINARY_FILE_UPLOAD</option>
                        <option value="code">RAW_SOURCE_CODE</option>
                    </select>
                </div>

                <div id="fileGroup" style="margin-bottom: 2.5rem;">
                    <label class="mono" style="display: block; margin-bottom: 1rem; margin-left: 1.5rem;">SELECT_ASSET_FILE</label>
                    <div style="border: 1px dashed var(--zinc-800); border-radius: 2rem; padding: 2rem; text-align: center;">
                        <input type="file" name="projectFile" style="color: var(--zinc-400);">
                    </div>
                </div>

                <div id="codeGroup" style="margin-bottom: 2.5rem; display: none;">
                    <label class="mono" style="display: block; margin-bottom: 1rem; margin-left: 1.5rem;">CODE_BUFFER_STREAM</label>
                    <textarea name="projectCode" class="cyber-input" rows="10" placeholder="PASTE_SOURCE_HERE..." style="border-radius: 2rem;"></textarea>
                </div>

                <div style="margin-bottom: 2.5rem;">
                    <label class="mono" style="display: block; margin-bottom: 1rem; margin-left: 1.5rem;">TECHNICAL_DOCUMENTATION</label>
                    <textarea name="note" class="cyber-input" rows="3" placeholder="OPTIONAL_NOTES_"></textarea>
                </div>

                <div style="margin-bottom: 3.5rem;">
                    <label class="mono" style="display: block; margin-bottom: 1rem; margin-left: 1.5rem;">INTERFACE_PREVIEW</label>
                    <input type="file" name="previewImg" class="cyber-input">
                </div>

                <button type="submit" class="btn-glow" style="width: 100%; font-size: 1.1rem; letter-spacing: 0.2em;">INITIALIZE_DEPLOYMENT</button>
            </form>
        </div>
    </div>

    <nav class="nav-floating">
        <a href="/" class="nav-link">EXIT_COMMAND</a>
    </nav>

    <script>
        function toggleInput() {
            const type = document.getElementById('typeSelect').value;
            document.getElementById('fileGroup').style.display = type === 'code' ? 'none' : 'block';
            document.getElementById('codeGroup').style.display = type === 'file' ? 'none' : 'block';
        }
    </script>
</body>
</html>
