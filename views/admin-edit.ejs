<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>IDENTITY_CONFIG // Core Override</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="app-container" style="max-width: 700px;">
        <header style="margin-bottom: 4rem; text-align: center;">
            <p class="mono" style="color: var(--accent); margin-bottom: 0.5rem;">[ IDENTITY_MODIFICATION_PROTOCOL ]</p>
            <h1>CORE_CONFIG</h1>
        </header>

        <div style="background: var(--zinc-900); border: 1px solid var(--zinc-800); border-radius: 4rem; padding: 4rem; position: relative;">
            <form action="/admin/edit-profile" method="POST" enctype="multipart/form-data">
                
                <!-- Visual Identity Section -->
                <div style="text-align: center; margin-bottom: 4rem;">
                    <div style="position: relative; display: inline-block;">
                        <div style="position: absolute; inset: -10px; border: 2px solid var(--accent); border-radius: 50%; border-style: dashed; opacity: 0.5;"></div>
                        <img src="<%= admin.profilePic || 'https://api.dicebear.com/7.x/shapes/svg?seed=' + admin.username %>" 
                             style="width: 150px; height: 150px; border-radius: 50%; background: var(--bg); border: 1px solid var(--zinc-800); object-fit: cover;">
                    </div>
                    <label class="mono" style="display: block; margin-top: 2rem; color: var(--zinc-400); font-size: 0.7rem; cursor: pointer;">
                        [ UPLOAD_NEW_BIO_DATA_IMAGE ]
                        <input type="file" name="profilePic" style="display: none;">
                    </label>
                </div>

                <!-- Field: Bio Name -->
                <div style="margin-bottom: 2rem;">
                    <label class="mono" style="display: block; margin-bottom: 0.8rem; margin-left: 1.5rem;">ADMIN_IDENTIFIER_</label>
                    <input type="text" name="name" value="<%= admin.name %>" class="cyber-input" required>
                </div>

                <!-- Field: Quote -->
                <div style="margin-bottom: 2rem;">
                    <label class="mono" style="display: block; margin-bottom: 0.8rem; margin-left: 1.5rem;">QUOTATION_ENCRYPTION_</label>
                    <textarea name="quote" class="cyber-input" rows="3" style="border-radius: 2rem;"><%= admin.quote %></textarea>
                </div>

                <!-- Field: Hashtags -->
                <div style="margin-bottom: 4rem;">
                    <label class="mono" style="display: block; margin-bottom: 0.8rem; margin-left: 1.5rem;">TAG_METADATA_</label>
                    <input type="text" name="hashtag" value="<%= admin.hashtag %>" class="cyber-input">
                </div>

                <!-- Security Override Section -->
                <div style="border-top: 1px solid var(--zinc-800); padding-top: 3rem;">
                    <h2 class="mono" style="color: var(--accent); margin-bottom: 2rem; font-size: 0.8rem;">[ SECURITY_OVERRIDE ]</h2>
                    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem;">
                        <div>
                            <label class="mono" style="display: block; margin-bottom: 0.8rem; margin-left: 1.5rem;">OLD_KEY_</label>
                            <input type="password" name="oldPassword" class="cyber-input" placeholder="••••••••">
                        </div>
                        <div>
                            <label class="mono" style="display: block; margin-bottom: 0.8rem; margin-left: 1.5rem;">NEW_KEY_</label>
                            <input type="password" name="newPassword" class="cyber-input" placeholder="••••••••">
                        </div>
                    </div>
                </div>

                <button type="submit" class="btn-glow" style="width: 100%; margin-top: 4rem;">EXECUTE_MODIFICATION</button>
            </form>
        </div>
    </div>

    <nav class="nav-floating">
        <a href="/admin/upload" class="nav-link">ABORT</a>
    </nav>
</body>
</html>
