<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IDENTITY_EDITOR | Admin</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&family=Press+Start+2P&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .edit-container {
            max-width: 700px;
            margin: 50px auto;
            background: #0A0A0A;
            border: 4px solid #FF0000;
            padding: 40px;
            box-shadow: 15px 15px 0px #111;
        }
        .section-title {
            font-family: 'Press Start 2P';
            font-size: 0.8rem;
            color: #FF0000;
            margin: 30px 0 15px;
            border-bottom: 1px solid #333;
            padding-bottom: 10px;
        }
        .current-pic {
            width: 100px;
            height: 100px;
            border: 2px solid #FF0000;
            margin-bottom: 15px;
            object-fit: cover;
        }
        .form-label {
            display: block;
            font-family: 'Press Start 2P';
            font-size: 0.5rem;
            color: #666;
            margin-bottom: 8px;
        }
        .cyber-input {
            width: 100%;
            background: #000;
            border: 1px solid #333;
            padding: 12px;
            color: white;
            margin-bottom: 20px;
            outline: none;
        }
        .cyber-input:focus {
            border-color: #FF0000;
        }
        .btn-group {
            display: flex;
            gap: 15px;
            margin-top: 30px;
        }
    </style>
</head>
<body>
    <header>
        <div class="logo">IDENTITY_CONFIG</div>
        <a href="/admin/upload" class="btn-modern btn-outline">CANCEL</a>
    </header>

    <div class="edit-container">
        <h2 style="font-family: 'Press Start 2P'; font-size: 1rem; margin-bottom: 30px;">MODIFY_ADMIN_DATA</h2>

        <form action="/admin/edit-profile" method="POST" enctype="multipart/form-data">
            
            <div class="section-title">VISUAL_ID</div>
            <img src="<%= admin.profilePic || 'https://api.dicebear.com/7.x/pixel-art/svg?seed=' + admin.username %>" class="current-pic">
            <label class="form-label">UPLOAD_NEW_AVATAR_</label>
            <input type="file" name="profilePic" class="cyber-input" style="border-style: dashed;">

            <div class="section-title">PUBLIC_INFO</div>
            <label class="form-label">DISPLAY_NAME_</label>
            <input type="text" name="name" value="<%= admin.name %>" class="cyber-input" required>
            
            <label class="form-label">PERSONAL_QUOTE_</label>
            <textarea name="quote" class="cyber-input" rows="3"><%= admin.quote %></textarea>
            
            <label class="form-label">TAGS_ (Space separated)</label>
            <input type="text" name="hashtag" value="<%= admin.hashtag %>" class="cyber-input">

            <div class="section-title">SECURITY_OVERRIDE</div>
            <p style="font-size: 0.7rem; color: #666; margin-bottom: 15px;">*Fill both to change password, leave empty to keep current.</p>
            <label class="form-label">OLD_PASSWORD_</label>
            <input type="password" name="oldPassword" class="cyber-input">
            <label class="form-label">NEW_PASSWORD_</label>
            <input type="password" name="newPassword" class="cyber-input">

            <div class="btn-group">
                <button type="submit" class="btn-modern" style="flex: 2;">SAVE_CHANGES</button>
            </div>
        </form>
    </div>
</body>
</html>
