<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ADMIN_AUTH | Source Code</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&family=Press+Start+2P&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .login-wrapper {
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        .login-card {
            width: 100%;
            max-width: 400px;
            background: #0A0A0A;
            border: 4px solid #FF0000;
            padding: 40px;
            box-shadow: 15px 15px 0px #111;
        }
        .login-header {
            font-family: 'Press Start 2P';
            font-size: 0.9rem;
            color: #FF0000;
            text-align: center;
            margin-bottom: 30px;
            letter-spacing: -1px;
        }
        .input-group {
            margin-bottom: 20px;
        }
        .input-group label {
            display: block;
            font-family: 'Press Start 2P';
            font-size: 0.5rem;
            color: #666;
            margin-bottom: 10px;
            text-transform: uppercase;
        }
        .login-input {
            width: 100%;
            background: #000;
            border: 2px solid #333;
            padding: 15px;
            color: white;
            font-family: 'Roboto', sans-serif;
            outline: none;
            transition: 0.3s;
        }
        .login-input:focus {
            border-color: #FF0000;
        }
        .status-text {
            text-align: center;
            font-size: 0.7rem;
            color: #444;
            margin-top: 20px;
            font-family: 'Press Start 2P';
        }
    </style>
</head>
<body>
    <div class="login-wrapper">
        <div class="login-card">
            <div class="login-header">ADMIN_LOGIN_</div>
            
            <form action="/login" method="POST">
                <div class="input-group">
                    <label>Identification_</label>
                    <input type="text" name="username" class="login-input" placeholder="USERNAME" required>
                </div>
                
                <div class="input-group">
                    <label>Security_Key_</label>
                    <input type="password" name="password" class="login-input" placeholder="PASSWORD" required>
                </div>
                
                <button type="submit" class="btn-modern" style="width: 100%; margin-top: 10px;">AUTHORIZE</button>
            </form>

            <div class="status-text">SYSTEM_READY</div>
        </div>
    </div>
</body>
</html>
