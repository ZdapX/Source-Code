<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>AUTH_ // Identity Verification</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body style="display: flex; align-items: center; justify-content: center; height: 100vh; padding: 20px;">
    
    <div style="width: 100%; max-width: 450px;">
        <div style="text-align: center; margin-bottom: 3rem;">
            <p class="mono" style="color: var(--accent); letter-spacing: 0.3em; margin-bottom: 0.5rem;">[ SECURITY_GATE_01 ]</p>
            <h1 style="font-size: 2.5rem;">AUTHORIZE</h1>
        </div>

        <div style="background: var(--zinc-900); border: 1px solid var(--zinc-800); border-radius: 3rem; padding: 3.5rem; position: relative;">
            <!-- Decorative Accent -->
            <div style="position: absolute; top: 2rem; right: 2rem; width: 8px; height: 8px; background: var(--accent); border-radius: 50%;"></div>
            
            <form action="/login" method="POST">
                <div style="margin-bottom: 1.5rem;">
                    <label class="mono" style="display: block; margin-bottom: 0.8rem; margin-left: 1rem;">IDENTIFIER_</label>
                    <input type="text" name="username" class="cyber-input" placeholder="USER_ID" required>
                </div>

                <div style="margin-bottom: 2.5rem;">
                    <label class="mono" style="display: block; margin-bottom: 0.8rem; margin-left: 1rem;">ACCESS_KEY_</label>
                    <input type="password" name="password" class="cyber-input" placeholder="••••••••" required>
                </div>

                <button type="submit" class="btn-glow" style="width: 100%;">ESTABLISH_SESSION</button>
            </form>
        </div>

        <p class="mono" style="text-align: center; margin-top: 2rem; opacity: 0.3; font-size: 0.7rem;">
            ENCRYPTED_END_TO_END // ACCESS_LOGGED
        </p>
    </div>

    <nav class="nav-floating">
        <a href="/" class="nav-link">ABORT_PROTOCOL</a>
    </nav>
</body>
</html>
