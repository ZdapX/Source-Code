<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ADMIN_PROFILES | Source Code</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&family=Press+Start+2P&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .profile-wrapper {
            max-width: 1100px;
            margin: 60px auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 40px;
            padding: 0 20px;
        }
        .profile-card {
            background: #0A0A0A;
            border: 4px solid #FF0000;
            padding: 40px;
            position: relative;
            box-shadow: 12px 12px 0px #222;
        }
        .profile-card::before {
            content: "ADMIN_ACCESS";
            position: absolute;
            top: -15px;
            left: 20px;
            background: #FF0000;
            color: white;
            padding: 2px 10px;
            font-family: 'Press Start 2P';
            font-size: 0.6rem;
        }
        .avatar {
            width: 120px;
            height: 120px;
            background: #111;
            border: 3px solid #FF0000;
            margin-bottom: 25px;
            object-fit: cover;
        }
        .admin-name {
            font-family: 'Press Start 2P';
            font-size: 1rem;
            color: #FF0000;
            margin-bottom: 20px;
            text-transform: uppercase;
        }
        .admin-quote {
            font-style: italic;
            color: #EEE;
            line-height: 1.8;
            margin-bottom: 25px;
            border-left: 3px solid #FF0000;
            padding-left: 15px;
        }
        .admin-hashtags {
            color: #FF0000;
            font-family: 'Press Start 2P';
            font-size: 0.55rem;
            word-spacing: 5px;
        }
    </style>
</head>
<body>
    <header>
        <div class="logo">STAFF_ONLY</div>
        <a href="/" class="btn-modern btn-outline">[ RETURN_TO_BASE ]</a>
    </header>

    <div class="profile-wrapper">
        <% admins.forEach(admin => { %>
        <div class="profile-card">
            <img src="<%= admin.profilePic || 'https://api.dicebear.com/7.x/pixel-art/svg?seed=' + admin.username %>" class="avatar">
            <h2 class="admin-name"><%= admin.name %></h2>
            <p class="admin-quote">"<%= admin.quote %>"</p>
            <div class="admin-hashtags">
                <%= admin.hashtag %>
            </div>
        </div>
        <% }) %>
    </div>
</body>
</html>
