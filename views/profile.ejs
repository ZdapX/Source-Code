<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>IDENTITIES_ // Core Personnel</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="app-container">
        <header style="margin-bottom: 5rem; text-align: center;">
            <p class="mono" style="color: var(--accent);">[ PERSONNEL_DIRECTORY ]</p>
            <h1>CORE_IDENTITIES</h1>
        </header>

        <div style="display: flex; flex-direction: column; gap: 2rem;">
            <% admins.forEach(admin => { %>
            <div class="project-card" style="border-radius: 4rem; padding: 3rem; cursor: default;">
                <div style="display: flex; align-items: center; gap: 3rem; width: 100%;">
                    
                    <!-- Avatar with Tech Circle -->
                    <div style="position: relative; flex-shrink: 0;">
                        <div style="position: absolute; inset: -10px; border: 1px solid var(--accent); border-radius: 50%; opacity: 0.5;"></div>
                        <img src="<%= admin.profilePic || 'https://api.dicebear.com/7.x/shapes/svg?seed=' + admin.username %>" 
                             style="width: 120px; height: 120px; border-radius: 50%; background: var(--zinc-800); object-fit: cover; display: block;">
                    </div>

                    <div style="flex: 1;">
                        <div class="card-meta" style="margin-bottom: 1rem;">
                            <span class="badge">LVL_ADMIN</span>
                            <span class="mono">STATUS: ACTIVE</span>
                        </div>
                        <h2 style="font-size: 2rem; margin-bottom: 1rem;"><%= admin.name %></h2>
                        <p style="color: var(--zinc-400); font-size: 1.1rem; max-width: 600px; line-height: 1.6;">
                            "<%= admin.quote %>"
                        </p>
                        <div class="mono" style="color: var(--accent); margin-top: 1.5rem; font-size: 0.75rem;">
                            <%= admin.hashtag %>
                        </div>
                    </div>

                    <div class="stat-group">
                        <span class="stat-label">Verification</span>
                        <span class="stat-value" style="color: var(--accent);">SIGNED_</span>
                    </div>
                </div>
            </div>
            <% }) %>
        </div>
    </div>

    <nav class="nav-floating">
        <a href="/" class="nav-link">DASHBOARD</a>
        <a href="/chat-pilih" class="nav-link">COMMS</a>
    </nav>
</body>
</html>
